apiVersion: ark.mckinsey.com/v1alpha1
kind: Agent
metadata:
  name: transient-test-agent
spec:
  modelRef:
    name: test-model
  prompt: You are a test agent for transient error retry functionality.
