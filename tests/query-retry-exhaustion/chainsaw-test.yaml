apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: query-retry-exhaustion
  labels:
    standard: "true"
spec:
  description: Test query retry policy exhausts all retries and ends in error
  steps:
  - name: setup-resources
    try:
    - script:
        content: |
          helm install mock-llm oci://ghcr.io/dwmkerr/charts/mock-llm \
            --version 0.1.27 \
            --namespace $NAMESPACE \
            --values mock-llm-values.yaml
        env:
        - name: NAMESPACE
          value: ($namespace)
    - script:
        content: |
          helm install ark-tenant ../../charts/ark-tenant --namespace $NAMESPACE --create-namespace --wait
        env:
        - name: NAMESPACE
          value: ($namespace)
    - assert:
        resource:
          apiVersion: ark.mckinsey.com/v1alpha1
          kind: Model
          metadata:
            name: test-model
          status:
            conditions:
            - type: ModelAvailable
              status: "True"
    - apply:
        file: manifests/*.yaml

  - name: wait-for-query-completion
    try:
    - wait:
        apiVersion: ark.mckinsey.com/v1alpha1
        kind: Query
        name: retry-exhaustion-query
        timeout: 120s
        for:
          condition:
            name: Completed
            value: 'True'

  - name: validate-retry-exhaustion
    try:
    - assert:
        timeout: 1s
        resource:
          apiVersion: ark.mckinsey.com/v1alpha1
          kind: Query
          metadata:
            name: retry-exhaustion-query
          status:
            phase: error
            (retryCount >= `2`): true
    catch:
    - describe:
        apiVersion: ark.mckinsey.com/v1alpha1
        kind: Query
        name: retry-exhaustion-query
    - events: {}
