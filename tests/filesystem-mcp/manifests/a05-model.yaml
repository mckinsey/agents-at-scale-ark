apiVersion: ark.mckinsey.com/v1alpha1
kind: Model
metadata:
  name: test-model
spec:
  type: openai
  model:
    value: gpt-mock
  pollInterval: 3s
  config:
    openai:
      baseUrl:
        value: http://mock-llm.($namespace).svc.cluster.local:8080/v1
      apiKey:
        value: mock-api-key
