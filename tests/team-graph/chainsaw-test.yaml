apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: team-graph
spec:
  steps:
  - name: setup-test-environment
    try:
    - script:
        skipLogOutput: true
        content: |
          set -u
          echo "{\"token\": \"$E2E_TEST_AZURE_OPENAI_KEY\", \"url\": \"$E2E_TEST_AZURE_OPENAI_BASE_URL\"}"
        outputs:
        - name: azure
          value: (json_parse($stdout))
    - script:
        content: |
          helm install ark-tenant ../../charts/ark-tenant --namespace $NAMESPACE --create-namespace --wait
        env:
        - name: NAMESPACE
          value: ($namespace)
    - apply:
        file: manifests/*.yaml
  - name: wait-for-query-completion
    try:
    - assert:
        resource:
          apiVersion: ark.mckinsey.com/v1alpha1
          kind: Query
          metadata:
            name: test-graph-team
          status:
            phase: done
        timeout: 180s
    - assert:
        resource:
          apiVersion: ark.mckinsey.com/v1alpha1
          kind: Query
          metadata:
            name: test-graph-team
          status:
            (conditions[?type == 'Completed']):
            - status: 'True'
        timeout: 60s
    catch:
    - events: {}
    - describe:
        apiVersion: ark.mckinsey.com/v1alpha1
        kind: Query
        name: test-graph-team
    - script:
        content: |
          echo "Query status snapshot:"
          kubectl get query test-graph-team -n $NAMESPACE -o yaml || true
          echo "Recent query events:"
          kubectl get events -n $NAMESPACE --field-selector involvedObject.kind=Query,involvedObject.name=test-graph-team || true

  - name: validate-graph-execution
    try:
    - assert:
        resource:
          apiVersion: ark.mckinsey.com/v1alpha1
          kind: Query
          metadata:
            name: test-graph-team
          status:
            (response != null): true
            (response.target.type): team
            (response.target.name): graph-team
            (length(response.content) > `500`): true
    catch:
    - events: {}
    - describe:
        apiVersion: ark.mckinsey.com/v1alpha1
        kind: Query
        name: test-graph-team
    - script:
        content: |
          echo "Validation failure query status snapshot:"
          kubectl get query test-graph-team -n $NAMESPACE -o yaml || true
          echo "Recent query events:"
          kubectl get events -n $NAMESPACE --field-selector involvedObject.kind=Query,involvedObject.name=test-graph-team || true
    cleanup:
    - script:
        content: |
          helm uninstall ark-tenant --namespace $NAMESPACE --wait --timeout=180s || true
        env:
        - name: NAMESPACE
          value: ($namespace)
