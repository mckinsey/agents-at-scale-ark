apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: test-query-ttl-timeout
spec:
  steps:
  - name: setup-mock-llm
    try:
    - script:
        timeout: 180s
        content: |
          bash ../shared/install-mock-llm.sh
        env:
        - name: NAMESPACE
          value: ($namespace)

  - name: wait-for-model-ready
    try:
    - wait:
        apiVersion: ark.mckinsey.com/v1alpha1
        kind: Model
        name: test-model-mock
        timeout: 90s
        for:
          condition:
            name: ModelAvailable
            value: 'True'

  - name: run-query
    try:
    - apply:
        file: manifests/a03-agent.yaml
    - apply:
        file: manifests/a04-query.yaml
    - wait:
        apiVersion: ark.mckinsey.com/v1alpha1
        kind: Query
        name: test-query-math-timeout
        timeout: 2m
        for:
          condition:
            name: Completed
            value: 'True'
    - assert:
        resource:
          apiVersion: ark.mckinsey.com/v1alpha1
          kind: Query
          metadata:
            name: test-query-math-timeout
          status:
            phase: error
            # spec.timeout is now enforced by the controller as an SLO from
            # creationTime (#2597 sub-item 2). A 1ms budget elapses before any
            # A2A dispatch, so the terminal state carries a TimedOut* condition
            # reason and the timeout message in response.content — replacing
            # the pre-fix pattern where an in-flight A2A call surfaced
            # "context deadline exceeded" via the executor's error path.
            (contains(response.content, 'timed out')): true
    catch:
    - events: {}
    - describe:
        apiVersion: ark.mckinsey.com/v1alpha1
        kind: Query
        name: test-query-math-timeout

    cleanup:
    - script:
        content: |
          helm uninstall mock-llm --namespace $NAMESPACE --wait --timeout=180s || true
        env:
        - name: NAMESPACE
          value: ($namespace)
