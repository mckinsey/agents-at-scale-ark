apiVersion: ark.mckinsey.com/v1alpha1
kind: Model
metadata:
  name: test-model-openai
spec:
  config:
    openai:
      apiKey:
        value: mock-api-key
      baseUrl:
        value: http://mock-llm.($namespace).svc.cluster.local:8080/v1
  model:
    value: gpt-mock
  pollInterval: 3s
  type: openai
