apiVersion: ark.mckinsey.com/v1alpha1
kind: Agent
metadata:
  name: test-agent
spec:
  modelRef:
    name: test-model
  prompt: |
    You are a test agent. Respond with a short confirmation that the system is working.
