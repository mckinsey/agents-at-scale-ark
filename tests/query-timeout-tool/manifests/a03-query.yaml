apiVersion: ark.mckinsey.com/v1alpha1
kind: Query
metadata:
  name: slow-tool-query
spec:
  input: "Use the slow-tool to sleep for 5 seconds"
  timeout: "30s"
  target:
    type: agent
    name: slow-agent
