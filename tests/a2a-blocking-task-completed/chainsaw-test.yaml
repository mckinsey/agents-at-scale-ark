apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: a2a-blocking-task-completed
spec:
  description: Test A2A agent with blocking Task that completes successfully
  steps:
  - name: step-1
    try:
    # Install mock-llm with A2A agents enabled
    - script:
        timeout: 180s
        content: |
          bash ../shared/install-mock-llm.sh
        env:
        - name: NAMESPACE
          value: ($namespace)
    # Wait for countdown A2AServer to be Ready
    - assert:
        resource:
          apiVersion: ark.mckinsey.com/v1prealpha1
          kind: A2AServer
          metadata:
            name: mock-llm-countdown
          status:
            (contains(conditions[?type == 'Ready'].status || `[]`, 'True')): true
    # Apply Query targeting countdown agent
    - apply:
        file: manifests/a01-query.yaml
    - wait:
        apiVersion: ark.mckinsey.com/v1alpha1
        kind: Query
        name: countdown-test-query
        timeout: 60s
        for:
          condition:
            name: Completed
            value: 'True'
    # Verify response contains the final result artifact; intermediate task status updates are excluded from reply content
    - assert:
        timeout: 1s
        resource:
          apiVersion: ark.mckinsey.com/v1alpha1
          kind: Query
          metadata:
            name: countdown-test-query
          status:
            (response != null): true
            (response.target.name): 'countdown-agent'
            (contains(response.content, 'Countdown complete!')): true
            (contains(response.content, 'Starting countdown')): false
            (contains(response.content, 'seconds remaining')): false
    catch:
    - events: {}
    - describe:
        apiVersion: ark.mckinsey.com/v1prealpha1
        kind: A2AServer
        name: mock-llm-countdown
    - describe:
        apiVersion: ark.mckinsey.com/v1alpha1
        kind: Query
        name: countdown-test-query

    cleanup:
    - script:
        content: |
          helm uninstall mock-llm --namespace $NAMESPACE --wait --timeout=180s || true
        env:
        - name: NAMESPACE
          value: ($namespace)
