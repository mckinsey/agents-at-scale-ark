apiVersion: ark.mckinsey.com/v1alpha1
kind: Model
metadata:
  name: default
spec:
  type: openai
  model:
    value: gpt-4.1-mini
  pollInterval: 3s
  config:
    openai:
      baseUrl:
        value: http://mock-llm.($namespace).svc.cluster.local:8080/v1
      apiKey:
        value: mock-api-key

