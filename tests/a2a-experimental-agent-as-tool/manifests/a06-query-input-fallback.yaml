apiVersion: ark.mckinsey.com/v1alpha1
kind: Query
metadata:
  name: a2a-delegation-input-fallback
  annotations:
    ark.mckinsey.com/a2a-experimental-enabled: "true"
spec:
  input: "[scenario-input-fallback] Delegate using legacy input fallback"
  target:
    type: agent
    name: coordinator-agent-a2a
