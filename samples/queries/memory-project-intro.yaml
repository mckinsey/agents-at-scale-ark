apiVersion: ark.mckinsey.com/v1alpha1
kind: Query
metadata:
  name: memory-project-intro
spec:
  input: "I'm starting a new project called 'EcoTracker' - a mobile app to help users track their carbon footprint. The tech stack will be React Native for mobile, Node.js for backend, and PostgreSQL for the database. The target launch date is Q2 2025."
  targets:
    - type: agent
      name: technical-expert
  sessionId: project-ecotracker-dev