apiVersion: ark.mckinsey.com/v1alpha1
kind: Model
metadata:
  name: claude-sonnet
spec:
  type: anthropic
  model:
    value: claude-sonnet-4-20250514
  config:
    openai:
      apiKey:
        valueFrom:
          secretKeyRef:
            name: anthropic-api-key
            key: api-key
