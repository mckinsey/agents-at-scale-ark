name: kubernetes-mcp-server
description: Read-only kubernetes-mcp-server registered with Ark for grounding agents on cluster resources
supports:
  - dev
  - install
  - uninstall
commands:
  install:
    - make kubernetes-mcp-server-install
  dev:
    - make kubernetes-mcp-server-dev
