[project]
name = "tool"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
pip = ">=25.0.1,<26"
setuptools = ">=70.0.0"

dependencies = [
    "fastmcp>=2.9.0",
]
