---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: arkconfigs.ark.mckinsey.com
spec:
  group: ark.mckinsey.com
  names:
    kind: ArkConfig
    listKind: ArkConfigList
    plural: arkconfigs
    singular: arkconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.queryTTL
      name: QueryTTL
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ArkConfig is the Schema for cluster-wide Ark defaults.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ArkConfigSpec defines cluster-wide Ark defaults. The singleton
              object named "default" is consulted by admission webhooks when
              a Query is created without an explicit ttl.
            properties:
              queryTTL:
                description: |-
                  QueryTTL is the default TTL injected into Query resources
                  that do not specify spec.ttl. If unset, the hardcoded
                  fallback of 720h is used.
                type: string
            type: object
          status:
            description: ArkConfigStatus is reserved for future status reporting.
              Currently empty.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
