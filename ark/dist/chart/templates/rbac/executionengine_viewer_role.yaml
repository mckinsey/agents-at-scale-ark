{{- if .Values.rbac.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  name: executionengine-viewer-role
rules:
- apiGroups:
  - ark.mckinsey.com
  resources:
  - executionengines
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ark.mckinsey.com
  resources:
  - executionengines/status
  verbs:
  - get
{{- end -}}
