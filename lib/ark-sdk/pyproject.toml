[project]
name = "ark-sdk-builder"
version = "0.1.46"
requires-python = ">= 3.12"
dependencies = [
    "pip>=25.0",
    "build",
    "pytest",
    "pyyaml",
    "kubernetes",
    "pydantic"
]

[tool.setuptools]
py-modules = []