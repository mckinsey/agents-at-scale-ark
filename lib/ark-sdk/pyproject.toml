[build-system]
requires = ["setuptools>=64.0", "pip>=25.0"]
build-backend = "setuptools.build_meta"


[project]
name = "ark-sdk-builder"
version = "0.1.46"
requires-python = ">= 3.12"
dependencies = [
    "build",
    "pytest",
    "pyyaml",
    "kubernetes",
    "pydantic"
]

[tool.setuptools]
py-modules = []