[project]
name = "ark-sdk-builder"
version = "0.1.46"
requires-python = ">= 3.12"
pip = ">=25.0.1,<26"
setuptools = ">=70.0.0"

dependencies = [
    "build",
    "pytest",
    "pyyaml",
    "kubernetes",
    "pydantic"
]

[tool.setuptools]
py-modules = []