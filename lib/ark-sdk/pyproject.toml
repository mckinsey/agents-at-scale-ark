[project]
name = "ark-sdk-builder"
version = "0.1.68-rc.2"
requires-python = ">= 3.12"
dependencies = [
    "build",
    "pytest",
    "pyyaml",
    "kubernetes",
    "pydantic",
    "urllib3>=2.7.0",
]

[tool.setuptools]
py-modules = []
