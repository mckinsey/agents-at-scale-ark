# query-input-api-normalization

Add protocol-typed input accessors to `QuerySpec` alongside the existing OpenAI-typed `GetInputMessages`/`SetInputMessages`, enabling engines to receive input as `protocol.Message` without CRD schema changes.
