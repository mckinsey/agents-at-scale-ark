# Tasks: Simplify Query Controller Dispatch

- [x] **Task 1: Add context enrichment to completions handler** — In `handler.go` `setupExecution()`, add `WithQueryContext(ctx, string(query.UID), sessionId, query.Name)` and `WithA2AContextID` from `query.Annotations`. Unblocks A2A engine path through completions.
- [x] **Task 2: Simplify Agent.executeAgent in completions** — Remove `executeWithExecutionEngineRouter` and `executeWithNamedExecutionEngine` from `agent.go`. Simplify `executeAgent` to call `executeWithA2AExecutionEngine` directly when ExecutionEngine is set.
- [x] **Task 3: Delete ExecutionEngineA2AClient from completions** — Delete `ExecutionEngineA2AClient`, `resolveExecutionEngineAddress`, `extractResponseText`, `convertToExecutionEngineMessage`, `buildAgentConfig`, `buildParameters`, `detectProviderName`, `buildModelConfig`, `buildToolDefinitions` and related types from `execution_engine.go`. Delete corresponding tests.
- [x] **Task 4: Refactor controller dispatch to single path** — Add `resolveDispatchAddress` (3-way: a2a→completions, named→engine CRD, nil→completions). Generalize `executeViaEngine` to accept address param. Delete `shouldExecuteDirectly`, `executeDirectly`, `finalizeDirectStream`, `createSuccessResponse`, `serializeMessages`, `extractUserInput`, and streaming imports.
- [x] **Task 5: Verify end-to-end** — Run `cd ark && make test`. Confirm all query target types work, cancel/TTL flows unaffected.
