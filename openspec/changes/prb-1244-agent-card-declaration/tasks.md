# PR-B Agent Card Extension Declaration Tasks

## 1. Agent Card update

- [ ] 1.1 Add `ExecutionContextExtensionURI` to `Capabilities.Extensions` in `server.go` Agent Card construction.

## 2. Deprecation documentation

- [ ] 2.1 Add deprecation note on `ArkMetadataKey` constant in `ark/internal/a2a/a2a.go`.

## 3. Verification

- [ ] 3.1 Compile completions package.
- [ ] 3.2 Verify agent card endpoint returns the extension in capabilities (manual or integration test).
