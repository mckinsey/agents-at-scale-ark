# protocol-stream-contract

Introduce a protocol-native streaming event type alongside the existing `ChunkWithMetadata`, enabling the stream bridge between executor and controller to carry A2A-typed events.
