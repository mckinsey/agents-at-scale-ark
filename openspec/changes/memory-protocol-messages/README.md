# memory-protocol-messages

Step 7 in the staged protocol-native migration: add protocol-typed methods to `MemoryInterface` alongside the existing OpenAI-typed methods, with conversion at the HTTP wire boundary in `memory_http.go`.
