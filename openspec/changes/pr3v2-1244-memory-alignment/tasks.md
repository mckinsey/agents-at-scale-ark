# PR3-v2 Memory Alignment Tasks

## 1. Interface conversion

- [ ] 1.1 Update `MemoryInterface` to use `[]ProtocolMessage` for `AddMessages` and `GetMessages`.
- [ ] 1.2 Update HTTP memory implementation with adapter conversion at HTTP boundary.
- [ ] 1.3 Update noop memory implementation to store `[]ProtocolMessage` directly.

## 2. Handler integration

- [ ] 2.1 Update handler memory load/save paths for protocol-native types.

## 3. Verification

- [ ] 3.1 Compile completions package and tests.
- [ ] 3.2 Run focused memory and handler tests.
