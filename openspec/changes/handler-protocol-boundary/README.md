# handler-protocol-boundary

Invert the completions handler's conversion direction so that `buildA2AResponse` operates on protocol messages natively, with OpenAI conversion relegated to a compatibility adapter at the provider boundary.
